# Workspace packages are consumed as TypeScript source, so they must be linked.
link-workspace-packages=true
prefer-workspace-packages=true

# Fail loudly instead of silently resolving a peer from the root.
strict-peer-dependencies=false
auto-install-peers=true

# Keep the store deterministic in CI.
resolution-mode=highest
